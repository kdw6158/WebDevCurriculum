class Desktop {
  /* TODO: Desktop 클래스는 어떤 멤버함수와 멤버변수를 가져야 할까요? */
  constructor(set_icon, set_folder) {
    this.set_icon = set_icon;
    this.set_folder = set_folder;
  }

  get_icon() {
    return this.set_icon;
  }
  get_folder() {
    return this.set_folder;
  }
}

class Icon {
  /* TODO: Icon 클래스는 어떤 멤버함수와 멤버변수를 가져야 할까요? */
  constructor(name) {
    this.name = name;
  }
}

class Folder {
  /* TODO: Folder 클래스는 어떤 멤버함수와 멤버변수를 가져야 할까요? */
  constructor(name) {
    this.name = name;
  }
}

class Window1 {
  /* TODO: Window1 클래스는 어떤 멤버함수와 멤버변수를 가져야 할까요? */
}
